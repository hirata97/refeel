# CI/CD運用・保守ガイド

## 📋 目次

- [概要](#概要)
- [定期メンテナンス](#定期メンテナンス)
- [監視・アラート](#監視アラート)
- [コスト管理](#コスト管理)
- [継続的改善](#継続的改善)

## 概要

CI/CDパイプラインの安定運用と継続的改善のための運用・保守ガイドです。定期的なメンテナンス、監視、コスト管理の標準手順を定義します。

### 運用目標

- **可用性**: CI/CD実行成功率 > 95%
- **パフォーマンス**: PR品質ゲート実行時間 < 15分
- **セキュリティ**: High/Critical脆弱性0件維持
- **コスト効率**: GitHub Actions実行時間の最適化

## 定期メンテナンス

### 週次メンテナンス（毎週月曜日）

#### 1. CI/CD実行状況確認

```bash
# 直近1週間の実行履歴確認
gh run list --limit 50 --json conclusion,workflowName,createdAt > weekly-runs.json

# 成功率計算
cat weekly-runs.json | jq '[.[] | select(.conclusion=="success")] | length'
cat weekly-runs.json | jq 'length'

# 失敗ワークフロー分析
gh run list --limit 50 --status failure
```

**チェック項目**:

- [ ] 成功率95%以上維持
- [ ] 失敗原因の分析・対応
- [ ] 平均実行時間のトレンド確認

#### 2. 依存関係の脆弱性確認

```bash
# npm auditで脆弱性スキャン
npm audit --json > weekly-audit.json

# High/Critical脆弱性抽出
cat weekly-audit.json | jq -r '.vulnerabilities |
  to_entries[] |
  select(.value.severity=="high" or .value.severity=="critical")'

# 対応優先度判定
# Critical → 即座対応
# High → 今週中に対応
```

**対応手順**:

```bash
# 自動修正試行
npm audit fix

# 手動更新
npm update [package-name]

# 確認
npm audit --audit-level=high
```

#### 3. GitHub Actions実行時間分析

```bash
# 直近のワークフロー実行時間取得
gh run list --limit 20 --json workflowName,createdAt,updatedAt |
  jq '.[] | {
    workflow: .workflowName,
    duration: (.updatedAt | fromdateiso8601) - (.createdAt | fromdateiso8601)
  }'

# 平均実行時間計算
# 目標: PR Quality Gate < 15分
```

**改善アクション**:

- 15分超過 → 並行実行最適化検討
- 20分超過 → タイムアウト・ジョブ分割検討

### 月次メンテナンス（毎月1日）

#### 1. 依存関係の更新

```bash
# 古い依存関係確認
npm outdated

# 更新計画作成
# - patch版: 自動更新
# - minor版: 動作確認後更新
# - major版: 検証環境でテスト後更新

# 更新実行
npm update

# ロックファイル更新
npm install

# 全品質チェック
npm run ci:all
```

**更新スケジュール**:

| タイプ | 頻度 | 例 |
|--------|------|-----|
| **patch** (1.0.x) | 月次 | バグ修正 |
| **minor** (1.x.0) | 四半期 | 機能追加 |
| **major** (x.0.0) | 年次 | 破壊的変更 |

#### 2. ワークフロー設定レビュー

```bash
# ワークフローファイル一覧
ls -la .github/workflows/

# 各ワークフローのレビュー項目
# - タイムアウト設定の妥当性
# - 権限設定の最小化
# - キャッシュ戦略の有効性
# - 並行実行最適化
```

**レビューチェックリスト**:

- [ ] 不要なワークフローの削除
- [ ] タイムアウト設定の最適化
- [ ] paths-ignoreの見直し
- [ ] concurrency設定の確認

#### 3. ドキュメント更新

```bash
# CI/CDドキュメント更新確認
ls docs/INFRASTRUCTURE/

# 更新項目
# - 設定変更の反映
# - 新規ツール・手順の追記
# - トラブルシューティング事例追加
```

**更新対象**:

- `CI_CD_OVERVIEW.md` - アーキテクチャ変更時
- `CI_CD_CONFIGURATION.md` - 設定手順変更時
- `CI_CD_TROUBLESHOOTING.md` - 新規エラーパターン発見時
- `CI_CD_QUICK_REFERENCE.md` - コマンド・手順変更時

### 四半期メンテナンス（1月・4月・7月・10月）

#### 1. 包括的レビュー

```bash
# 過去3ヶ月のCI/CD統計分析
gh run list --limit 1000 --json conclusion,workflowName,createdAt,updatedAt > quarterly-stats.json

# 分析項目
# - 成功率トレンド
# - 実行時間トレンド
# - 失敗パターン分析
# - コスト推移
```

#### 2. パフォーマンス最適化

**実行時間最適化**:

- キャッシュ戦略見直し
- ジョブ並行実行最適化
- 不要ステップ削除

**コスト最適化**:

- タイムアウト短縮
- マトリックス戦略最適化
- セルフホストランナー検討

#### 3. セキュリティ監査

```bash
# Secrets棚卸し
gh secret list

# 使用状況確認
# - 未使用Secretsの削除
# - 定期ローテーション（6ヶ月ごと）
# - アクセス権限見直し
```

### 年次メンテナンス（1月）

#### 1. 技術スタック更新

**メジャーバージョンアップ**:

- Node.js LTSバージョン更新
- GitHub Actionsバージョン更新
- テストフレームワーク更新

**手順**:

```bash
# 1. 検証ブランチ作成
git checkout -b chore/annual-dependency-update

# 2. package.jsonメジャー更新
npm install vue@latest
npm install typescript@latest
npm install vitest@latest

# 3. 全品質チェック
npm run ci:all

# 4. E2Eテスト
npm run test:e2e

# 5. 問題なければPR作成
gh pr create --title "chore: annual dependency major update"
```

#### 2. CI/CD戦略見直し

- 品質ゲート基準の見直し
- 新技術・ツールの導入検討
- パフォーマンス目標の再設定
- コスト目標の再設定

## 監視・アラート

### リアルタイム監視

#### GitHub Actions Status

```bash
# 最新実行状況確認
gh run list --limit 5

# 失敗時の即座確認
gh run view [run-id] --log-failed
```

#### PR品質ゲートステータス

```bash
# オープンPRのCI/CD状況
gh pr list --json number,title,statusCheckRollup

# 特定PRのチェック状況
gh pr checks [PR番号]
```

### 成功率監視

#### 週次レポート自動生成

```bash
#!/bin/bash
# scripts/weekly-ci-report.sh

echo "## CI/CD Weekly Report ($(date +%Y-%m-%d))"
echo ""

# 直近1週間の実行統計
total=$(gh run list --limit 100 --json conclusion | jq 'length')
success=$(gh run list --limit 100 --json conclusion | jq '[.[] | select(.conclusion=="success")] | length')
failure=$(gh run list --limit 100 --json conclusion | jq '[.[] | select(.conclusion=="failure")] | length')

success_rate=$(echo "scale=2; $success * 100 / $total" | bc)

echo "**Success Rate:** $success_rate%"
echo "- Total: $total"
echo "- Success: $success"
echo "- Failure: $failure"
echo ""

# 目標達成判定
if (( $(echo "$success_rate < 95" | bc -l) )); then
  echo "⚠️ WARNING: Success rate below target (95%)"
else
  echo "✅ Success rate meets target"
fi
```

**実行スケジュール**:

```bash
# cron設定（毎週月曜9時）
0 9 * * 1 /path/to/scripts/weekly-ci-report.sh > weekly-report.md
```

### パフォーマンス監視

#### 実行時間トラッキング

```bash
# PR Quality Gate平均実行時間
gh run list --workflow="pr-quality-gate.yml" --limit 20 --json createdAt,updatedAt |
  jq '[.[] | ((.updatedAt | fromdateiso8601) - (.createdAt | fromdateiso8601))] |
  add / length / 60' # 分単位

# 目標: 15分以内
# 警告: 18分以上
# アラート: 20分以上
```

### アラート設定

#### Slack通知設定（推奨）

```yaml
# .github/workflows/alert.yml

name: CI/CD Alert

on:
  workflow_run:
    workflows: ["PR Quality Gate"]
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}

    steps:
    - name: Send Slack notification
      uses: slackapi/slack-github-action@v1
      with:
        webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
        payload: |
          {
            "text": "❌ CI/CD Failed: ${{ github.event.workflow_run.name }}",
            "blocks": [
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": "*Workflow:* ${{ github.event.workflow_run.name }}\n*Status:* Failed\n*Branch:* ${{ github.event.workflow_run.head_branch }}"
                }
              }
            ]
          }
```

## コスト管理

### GitHub Actions使用量監視

#### 月次使用量確認

```bash
# GitHub CLI拡張（gh-actions-usage）インストール
gh extension install https://github.com/github/gh-actions-usage

# 使用量確認
gh actions-usage --repo [owner/repo]

# 月次レポート
# - 実行時間合計
# - ワークフロー別内訳
# - コスト推定
```

#### 使用量削減戦略

**並行実行最適化**:

```yaml
# 変更前: 順次実行（合計15分）
jobs:
  lint:
    runs-on: ubuntu-latest
  test:
    needs: lint
    runs-on: ubuntu-latest
  build:
    needs: test
    runs-on: ubuntu-latest

# 変更後: 並行実行（合計8分）
jobs:
  lint:
    runs-on: ubuntu-latest
  test:
    runs-on: ubuntu-latest
  build:
    needs: [lint, test]  # 並行実行
    runs-on: ubuntu-latest
```

**キャッシュ最大活用**:

```yaml
- name: Cache dependencies
  uses: actions/cache@v4
  with:
    path: |
      ~/.npm
      node_modules
      .vite
    key: ${{ runner.os }}-${{ hashFiles('package-lock.json') }}
    restore-keys: |
      ${{ runner.os }}-

# 効果: 3-5分 → 1-2分（60%削減）
```

**paths-ignore活用**:

```yaml
on:
  pull_request:
    paths-ignore:
      - 'docs/**'      # ドキュメント変更
      - '*.md'         # Markdownファイル
      - '.gitignore'   # Git設定
      - 'LICENSE'      # ライセンス

# 効果: 不要な実行を30-40%削減
```

### コスト目標設定

| 項目 | 目標値 | 現状 | 備考 |
|------|--------|------|------|
| **月次実行時間** | < 500分 | 監視中 | 無料枠2,000分 |
| **PR平均実行時間** | < 15分 | 8-12分 | 目標達成中 |
| **キャッシュヒット率** | > 80% | 監視中 | 高速化指標 |

### セルフホストランナー検討

**導入検討条件**:

- 月次実行時間 > 1,000分
- 実行頻度が高い（1日20回以上）
- 特殊な実行環境が必要

**コスト比較**:

- GitHub Hosted: $0.008/分（Linux）
- Self-Hosted: サーバー費用 + メンテナンス費用

## 継続的改善

### 改善サイクル（PDCAサイクル）

#### Plan（計画）

**月次改善目標設定**:

```markdown
## 2025年1月 CI/CD改善目標

1. **成功率向上**: 95% → 97%
2. **実行時間短縮**: 平均12分 → 10分
3. **カバレッジ向上**: 65% → 70%
```

#### Do（実行）

**改善施策実施**:

- キャッシュ戦略最適化
- 並行実行最適化
- テストケース追加

#### Check（評価）

**月末評価**:

```bash
# 成功率確認
# 実行時間確認
# カバレッジ確認

# 目標達成度評価
# - 達成: 次の目標設定
# - 未達成: 原因分析・対策見直し
```

#### Act（改善）

**次月アクション決定**:

- 達成施策の標準化
- 未達成項目の再計画
- 新規改善テーマ設定

### ベストプラクティス収集

#### 成功事例の共有

```markdown
## CI/CD改善事例

### 事例1: キャッシュ戦略最適化
**実施日**: 2025-01-15
**施策**: node_modules + .viteキャッシュ追加
**効果**: 実行時間 15分 → 10分（33%削減）
**再現手順**: [手順詳細]
```

#### 失敗事例の共有

```markdown
## CI/CD失敗事例

### 事例1: タイムアウト設定不足
**発生日**: 2025-01-10
**問題**: E2Eテストが10分タイムアウト
**原因**: 実行時間見積もり不足
**対策**: タイムアウト20分に延長
**予防策**: バッファ20%確保ルール策定
```

### 技術動向調査

#### 四半期技術調査

**調査項目**:

- GitHub Actions新機能
- テストツール最新動向
- CI/CDベストプラクティス
- セキュリティツール更新

**情報源**:

- GitHub Changelog
- npm weekly
- Dev.to CI/CDタグ
- Qiita CI/CDタグ

## 関連ドキュメント

- [CI/CDアーキテクチャ](./CI_CD_OVERVIEW.md)
- [設定変更手順](./CI_CD_CONFIGURATION.md)
- [トラブルシューティングガイド](./CI_CD_TROUBLESHOOTING.md)
- [クイックリファレンス](./CI_CD_QUICK_REFERENCE.md)

## バージョン履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.0.0 | 2025-01-14 | 初版作成 |
