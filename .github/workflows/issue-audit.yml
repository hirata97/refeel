name: Issue Management Audit

on:
  schedule:
    # 毎月1日の午前9時（JST）に実行
    - cron: '0 0 1 * *'
  workflow_dispatch:
    # 手動実行も可能

jobs:
  audit-issues:
    runs-on: ubuntu-latest
    permissions:
      issues: read
      contents: read
      pull-requests: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run issue audit
        run: npm run issue-audit
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload audit report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: issue-audit-report
          path: issue-audit-*.md
          retention-days: 30

      - name: Create issue comment if problems found
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            // Issue #69が存在するか確認
            try {
              const issue69 = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: 69
              });
              
              if (issue69.data.state === 'open') {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: 69,
                  body: `🚨 **定期監査で重要な問題を検出**

Issue管理の定期監査により、以下の問題が検出されました:
- テスト実行に失敗があります
- CI/CD品質チェックでエラーが発生している可能性があります

詳細な監査レポートは[Artifacts](${context.payload.workflow_run?.html_url || 'GitHub Actions'}）をご確認ください。

**推奨アクション**:
1. テスト失敗の原因を調査・修復
2. CI/CD品質チェックの確認
3. 関連する技術債務の整理

*このコメントは自動生成されました（Issue管理監査ワークフロー）*`
                });
              }
            } catch (error) {
              console.log('Issue #69 not found or already closed');
            }